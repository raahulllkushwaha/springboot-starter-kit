spring:
  datasource:
    url: jdbc:mysql://localhost:3306/starterkit_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: test
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    clean-on-validation-error: true    # ← yeh add karo
    url: jdbc:mysql://localhost:3306/starterkit_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    user: test
    password: test

app:
  jwt:
    secret: dGVzdC1zZWNyZXQta2V5LWZvci1kZXYtb25seQ==
  admin:
    email: admin@test.com
    password: Admin@1234